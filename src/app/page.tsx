import { LandingPage } from '@/components/landing/landing-page';

/**
 * OpenInsights Landing Page
 *
 * Modern, privacy-first landing page based on Sprint Linear Clone template.
 * Uses the unified design system with dark-first aesthetic.
 */
export default function Page() {
  return <LandingPage />;
}
